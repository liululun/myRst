# CLAUDE.md

本文件为 Claude Code 在本项目中工作时自动加载的上下文/约定说明。

## 项目简介

`myRst` 目前处于初始阶段，仅包含一个 PyCharm 生成的示例脚本 `main.py`。

## 开发约定

- 语言：Python
- 运行入口：`python main.py`
- 提交代码前请确认改动范围，避免无关重构

## 目录说明

详见根目录下的 `README.md`，其中说明了 `.claude/`、`.idea/` 等配置目录的作用。
