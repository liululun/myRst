---
name: example-agent
description: 示例子代理模板，可复制修改为项目专属的子代理。仅在用户明确要求时使用。
tools: Read, Grep, Glob
model: inherit
---

你是本项目的一个示例子代理（sub-agent）。

这里描述该子代理的职责、可用工具范围以及完成任务后应返回的结果格式。

复制本文件并重命名，即可定义一个新的项目专属子代理。
