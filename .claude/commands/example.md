---
description: 示例自定义斜杠命令，可作为模板复制修改
---

这是一个示例自定义命令。

使用方式：在 Claude Code 中输入 `/example` 即可触发本提示词。

可以在此处编写任意提示词内容，例如：
- 让 Claude 按固定步骤检查代码
- 生成项目周报
- 执行一段标准化的排查流程

复制本文件并重命名（例如 `deploy.md`），即可创建新的命令 `/deploy`。
